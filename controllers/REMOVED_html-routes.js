const db = require("../models");

module.exports = function(app) {
    app.get("/", function(req, res){
        //res.render("index");
        //Show homepage 
        //or get rid of this file?
    });
};